# WAA to calculate the area of a triangle

base = float(input("Enter the base: "))
height = float(input("Enter the height: "))

area = (base * height) / 2

print("Area of the triangle is:", area)